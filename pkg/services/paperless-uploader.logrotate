# place at /etc/logrotate.d/paperless-uploader

/var/log/paperless-uploader.log {
        size 100k
        missingok
        rotate 10
        notifempty
        create 0640 paperless-uploader paperless-uploader
}
